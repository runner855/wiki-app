export enum AppUrls  {
    COLLAPSE = "/collapse",
    SELECT = "/select",
    SEARCH = "/search",
    TRANSLATE = "/translate"
    
}

export enum AppUrls  {
    NETWORK = "/groups",
    CREATEGROUP = "/create-group",
    
}